export interface Headerdata {
    protocolName: string;
    country: string;
    currentClient: string;
    extensionCount: number;
    lastShipmentDate: Date;
    currentCredits: number;
    clientUsedCredits: number;
    totalUsedCredits: number;
}
